# 更新个人信息与 PDF 作品

## 最省事的更新方式

把你的脱敏 PDF 文件分别改成下面三个文件名，替换 `public/works` 文件夹中的示例文件：

- `solution-blueprint-sample.pdf`
- `field-discovery-sample.pdf`
- `production-readiness-sample.pdf`

替换后，网站里的“在线预览”和“下载 PDF”会自动使用你的文件。

## 更新照片、经历与案例文案

- 首页照片：替换 `public/justin-shanghai-portrait.jpg`，保持竖版照片即可
- 工作经历：修改两种语言下的 `experience.items`

这些内容主要集中在 `app/portfolio-content.ts`：

- 英文和中文文案：分别修改 `en` 与 `zh`
- 邮箱：搜索 `YOUR EMAIL HERE` 和 `请填入你的邮箱`
- 案例名称、摘要、角色：修改两种语言下的 `work.items`
- 能力介绍：修改两种语言下的 `capabilities.items`

姓名 `JUSTIN` 位于 `app/page.tsx`。网站首次访问默认显示英文，并会在浏览器中记住访客的语言选择。

建议仅上传已经脱敏、确认可公开展示的作品。
